version: "3.8"
services:
  backend:
    image: jaydeep2003/testify:latest
    container_name: backend
    env_file:
      - ./server/.env
    ports:
      - "4000:4000"

  frontend:
    build: ./client
    container_name: frontend
    depends_on:
      - backend
    ports:
      - "80:80"
